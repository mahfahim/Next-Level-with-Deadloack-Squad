# 18-7 Setting Up Prisma And DB Connection
# 18-8 Define Your First Data Model And Do DB Migration With Prisma Migrate And Prisma Client Generation With Prisma Generate
# 18-9 Write Your First Query

At first, 
1) git init
2) .gitignore -> node_modules

Doc Link: 
 https://www.prisma.io/docs/prisma-orm/quickstart/prisma-postgres

---------------------------------------------------------------


# ১. Project তৈরি

```bash
mkdir hello-prisma
cd hello-prisma
```

---

# ২. TypeScript Setup

```bash
npm init -y

npm install typescript tsx @types/node --save-dev

npx tsc --init
```

---

# ৩. Prisma এবং PostgreSQL Packages Install

```bash
npm install prisma @types/pg --save-dev

npm install @prisma/client @prisma/adapter-pg pg dotenv
```

### Package গুলোর কাজ

| Package            | কাজ                   |
| ------------------ | --------------------- |
| prisma             | Prisma CLI            |
| @prisma/client     | Database Query        |
| @prisma/adapter-pg | PostgreSQL Adapter    |
| pg                 | PostgreSQL Driver     |
| @types/pg          | TypeScript Types      |
| dotenv             | Environment Variables |

---

# ৪. tsconfig.json Update

```json
{
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "bundler",
    "target": "ES2023",
    "strict": true,
    "esModuleInterop": true,
    "ignoreDeprecations": "6.0",
    "types": ["node"],
    "outDir": "dist",
    "rootDir": "./"
  },
  // "include": ["src", "prisma.config.ts"],
  // "exclude": ["prisma.config.ts"]
}
```



| Option                        | কাজ                                                                    |
| ----------------------------- | ---------------------------------------------------------------------- |
| `module: "ESNext"`            | আধুনিক `import` / `export` ব্যবহার করবে।                               |
| `moduleResolution: "bundler"` | Vite, Next.js, Bun-এর মতো bundler অনুযায়ী package খুঁজবে।             |
| `target: "ES2023"`            | TypeScript কে ES2023 JavaScript-এ compile করবে।                        |
| `strict: true`                | বেশি type checking করবে, ভুল ধরতে সাহায্য করবে।                        |
| `esModuleInterop: true`       | CommonJS package সহজে import করতে দেবে।                                |
| `ignoreDeprecations: "6.0"`   | কিছু deprecation warning উপেক্ষা করবে।                                 |
| `types: ["node"]`             | Node.js এর built-in types (`fs`, `path`, `process`) ব্যবহার করতে দেবে। |
| `outDir: "dist"`              | Compile হওয়া `.js` ফাইল `dist` ফোল্ডারে যাবে।                          |
| `rootDir: "./"`               | Project root-কে source directory হিসেবে ধরবে।                          |

### Comment করা অংশ

```jsonc
// "include": ["src", "prisma.config.ts"]
```

শুধুমাত্র `src` এবং `prisma.config.ts` compile করবে।

```jsonc
// "exclude": ["prisma.config.ts"]
```

`prisma.config.ts` compile করবে না।

### এক লাইনে

এই `tsconfig.json` TypeScript-কে বলে:

> **"Modern JavaScript ব্যবহার করো, Node.js support দাও, strict type checking চালু রাখো, আর compile করা ফাইলগুলো `dist` ফোল্ডারে রাখো।"**

---

# ৫. package.json Update

```json
{
  "type": "module"
}
```



---

# ৬. Prisma Initialize

```bash
npx prisma init --output ../generated/prisma
```

---

# ৭. Folder Structure

এখন Project Structure হবে:

```text
hello-prisma/
│
├── generated/
│   └── prisma/
│
├── prisma/
│   └── schema.prisma
│
├── lib/
│   └── prisma.ts
│
├── script.ts
│
├── .env
│
├── prisma.config.ts
│
├── package.json
│
└── tsconfig.json
```

---

# ৮. prisma.config.ts

```ts

import "dotenv/config";
import { defineConfig } from "prisma/config";

export default defineConfig({
  schema: "prisma/schema.prisma",
  migrations: {
    path: "prisma/migrations",
  },
  datasource: {
    url: process.env["DATABASE_URL"],
  },
});

```

---

# ৯. Database তৈরি

Prisma Postgres ব্যবহার করলে:

```bash
npx create-db
```

Database তৈরি হলে একটি URL পাবেন।

উদাহরণ:

```env
DATABASE_URL="postgres://username:password@host:5432/dbname"
```

---

# ১০. .env File

```env
DATABASE_URL="postgres://username:password@host:5432/dbname"
```

---

# ১১. Prisma Schema

`prisma/schema.prisma`

```prisma
//prisma/schema.prisma

generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  
} 

model User {
  id Int  @id @default(autoincrement())
  email String @unique
  name String?
  posts Post[]
}

model Post {
  id Int @id @default(autoincrement())
  title String
  content String?
  published Boolean @default(false)
  authorId Int
  author User @relation(fields: [authorId], references: [id])
}

```

---

# ১২. Migration Run

Database Table Create হবে।

```bash
npx prisma migrate dev --name init
```

এতে Migration File এবং Database Table তৈরি হবে।

---

# ১৩. Prisma Client Generate

```bash
npx prisma generate
```

---

# ১৪. Prisma Connection File

### lib/prisma.ts

```ts
// lib/prisma.ts
import "dotenv/config";
import { PrismaPg } from "@prisma/adapter-pg";
import { PrismaClient } from "../generated/prisma/client";

const connectionString = `${process.env.DATABASE_URL}`;

const adapter = new PrismaPg({ connectionString });
const prisma = new PrismaClient({ adapter});

export { prisma };
```

---

# ১৫. Database Query File

### script.ts

```ts
// script.ts
import { title } from "node:process";
import { prisma } from "./lib/prisma";

async function main() {

    // const user = await prisma.user.create({
    //     data: {
    //         name : "Fahim",
    //         email : "fahim@gmail.com",
    //         posts : {
    //             create : {
    //                 title : "Hello world",
    //                 content : "This is my first post",
    //                 published : true,
    //             }
    //         }
    //     },
    //     include : {
    //         posts : true,
    //     }
    // });
    
    // console.log("Created User : ", user);

    const newPost = await prisma.post.create({
        data: {
            authorId : 1,
            title : "this is fahim",
            content : "this is my second post",
            published : true,
        },
    });

    console.log("Created post: ", newPost);

    const allUsers = await prisma.user.findMany({
        include: {
            posts: true,
        },
    });

    console.log("All users:", JSON.stringify(allUsers))
}

main()
    .then(async () => {
        await prisma.$disconnect();
    })
    .catch(async (e) => {
        console.error(e);
        await prisma.$disconnect();
        process.exit(1);
    })
```

---

# ১৬. Script Run

```bash
npx tsx script.ts
```

Output:

```bash
Created User:
{
  id: 1,
  name: 'Alice',
  email: 'alice@prisma.io'
}

All Users:
[
  {
    "id":1,
    "name":"Alice",
    "email":"alice@prisma.io",
    "posts":[
      {
        "id":1,
        "title":"Hello World"
      }
    ]
  }
]
```

---

# ১৭. Prisma Studio Open

Database GUI দেখতে:

```bash
npx prisma studio
```

Browser এ খুলবে:

```text
http://localhost:5555
```

এখান থেকে User, Post Table দেখতে ও Edit করতে পারবেন।

---

# Useful Prisma Commands

### Schema Change করলে

```bash
npx prisma migrate dev --name add_profile
```

---

### Client Regenerate

```bash
npx prisma generate
```

---

### Database Reset

```bash
npx prisma migrate reset
```

---

### Prisma Studio

```bash
npx prisma studio
```

---

# Final Project Structure

```text
hello-prisma/
│
├── generated/
│   └── prisma/
│       ├── client.ts
│       ├── models/
│       └── ...
│
├── prisma/
│   ├── schema.prisma
│   └── migrations/
│
├── lib/
│   └── prisma.ts
│
├── script.ts
│
├── .env
│
├── prisma.config.ts
│
├── package.json
│
├── tsconfig.json
│
└── node_modules/
```

এই স্ট্রাকচারটি Prisma + PostgreSQL + TypeScript-এর জন্য একটি সম্পূর্ণ Production-ready বেসিক সেটআপ। পরে এখান থেকে সহজেই Express.js, Next.js বা NestJS-এর সাথে Prisma যুক্ত করতে পারবেন।

